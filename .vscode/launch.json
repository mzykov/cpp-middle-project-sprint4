{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch Analyser",
            "program": "${workspaceFolder}/build/analyser",
            "args": [ "-f", "../files/sample.py" ],
            "cwd": "${workspaceFolder}/build"
        }
    ]
}